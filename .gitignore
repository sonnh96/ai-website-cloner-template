# Dependencies
node_modules/
.pnp
.pnp.js

# Build
.next/
out/
build/
dist/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Local settings (not project config)
.claude/settings.local.json

# User-specific skills (not part of the template)
.claude/skills/customize/
.claude/skills/release/

# impeccable skill install (per-user tooling, not part of the template) —
# installs itself across every harness dir it finds; only the impeccable
# subfolder is ignored so other harness skills (e.g. clone-website) stay tracked
.claude/skills/impeccable/
.agents/skills/impeccable/
.codex/skills/impeccable/
.cursor/skills/impeccable/
.gemini/skills/impeccable/
.github/skills/impeccable/
.opencode/skills/impeccable/
.codex/hooks.json
.cursor/hooks.json
.github/hooks/

# Local temp files
temp/
